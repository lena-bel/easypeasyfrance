{% extends "base.html.twig" %}

{% block body %}
    <main id="form">
        <a href="{{ path('task-content') }}" class="back-btn">Go back</a>
        {{ form_start(form) }}
        {{ form(form) }}
        {{ form_end(form) }}
    </main>
{% endblock %}