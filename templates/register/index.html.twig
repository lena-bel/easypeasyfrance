{% extends 'base.html.twig' %}


{% block body %}


<div class="example-wrapper">
    {{form_start(form)}}
        {{form_row(form)}}
        <button type='submit'>Register</button>
    {{form_end(form)}}

    
</div>
{% endblock %}
